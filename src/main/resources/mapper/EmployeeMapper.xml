<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.luying.dao.EmployeeDao" >
    <insert id="insertEmp" parameterType="employee">
        insert into employee (d_id,lastName,email,gender,id) values
       ( #{dId},#{lastName},#{email},#{gender},#{id})
    </insert>
    <delete id="deleteEmpById" parameterType="int">
         delete from employee where id=#{dId}
    </delete>


    <select id="getEmpById" parameterType="int" resultType="employee">
      select * from employee where id=#{id}
  </select>
    <update id="updateEmpById" parameterType="employee">

     update employee set lastName=#{lastName},
     gender=#{gender},email=#{email},
     d_id=#{dId} where id=#{id}
    </update>





    






</mapper>