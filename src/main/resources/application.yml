#debug: true
server:
  port: 8080
#  servlet:
#    context-path: /spboot
spring:
 profiles:
  active: dev
 messages:
    basename: i18n.login
 thymeleaf:
    cache: false
 mvc:
    date-format: yyyy-MM-dd
---
server:
  port: 8081
spring:
 profiles: prod
---
server:
  port: 8082
spring:
 profiles: dev
---
spring:
    datasource:
        name: mysql_test
        type: com.alibaba.druid.pool.DruidDataSource
        #druid相关配置
        druid:
          #监控统计拦截的filters
          filters: stat
          driver-class-name: com.mysql.jdbc.Driver
          #基本属性
          url: jdbc:mysql://127.0.0.1:3306/mytest?useUnicode=true&characterEncoding=UTF-8&allowMultiQueries=true
          username: root
          password: 123
          #配置初始化大小/最小/最大
          initial-size: 1
          min-idle: 1
          max-active: 20
          #获取连接等待超时时间
          max-wait: 60000
          #间隔多久进行一次检测，检测需要关闭的空闲连接
          time-between-eviction-runs-millis: 60000
          #一个连接在池中最小生存的时间
          min-evictable-idle-time-millis: 300000
          validation-query: SELECT 'x'
          test-while-idle: true
          test-on-borrow: false
          test-on-return: false
          #打开PSCache，并指定每个连接上PSCache的大小。oracle设为true，mysql设为false。分库分表较多推荐设置为false
          pool-prepared-statements: false
          max-pool-prepared-statement-per-connection-size: 20
    redis:
      host: 192.168.234.128
      port: 6379
      password: 123
    mail:
      username: 928229131@qq.com
      password: bivmwcbukmagbcba
      host: smtp.qq.com
      properties: {mail.smtp.ssl.enable: true}
#    rabbitmq:
#      addresses: 192.168.234.128
#      username: guest
#      password: guest
#      port:

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.luying.cache
# config-location: classpath:mapper/xx.xml
#pagehelper
pagehelper:
    helperDialect: mysql
    reasonable: true
    supportMethodsArguments: true
    params: count=countSql
    returnPageInfo: check
logging:
  level:
    com.luying.dao: debug
debug: true
#users:
#  name: 李剑
#  sex: 男
#  file: F:/springboot.log
#  pattern:
#    console: "%d{yyyy‐MM‐dd} *%thread* ‐ %msg%n"